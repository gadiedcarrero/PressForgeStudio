# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# PressForge output (generated reels & assets)
output/
!output/.gitkeep

# Datos locales de publicación (cola, canales con tokens) — nunca al repo
data/

# API keys del usuario (BYOK) — local a cada equipo, nunca al repo
secrets.json

# CLAVE PRIVADA de licencias — SOLO del vendedor, JAMÁS al repo
license_private_key.txt

# Cache local de muestras de voz de ElevenLabs (por voz, no al repo)
voice_previews/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Misc tooling
.qodo/

# Brand kits: ahora viven en STORAGE_DIR/branding (Drive), no en el repo
assets/branding/
